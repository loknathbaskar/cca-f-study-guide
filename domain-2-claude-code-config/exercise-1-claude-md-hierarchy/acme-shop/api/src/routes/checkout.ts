// Placeholder route file — exists so .claude/rules/api-rules.md has a real
// path to attach to. In a real repo this would be actual Express route code.
export const checkoutRoute = () => {
  // TODO: implement — Zod validation, ApiError shape, rateLimiter middleware
};
